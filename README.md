# hs_timer

## 介绍

- 该模块是基于 POSIX 定时器接口实现的通用定时器。
- 使用 `SIGEV_THREAD` 方式触发定时任务，回调函数运行在独立线程中。
- 定时器在生命周期结束时会自动完成资源释放，无需用户显式销毁。
- 定时器采用串行触发机制，确保同一定时器的回调函数不会发生并发或重入。

## 使用说明

- 具体使用方式参考[示例代码](https://github.com/hu-submodule-demo/hs_timer_demo)
- 编译时需要添加`-lrt`选项
